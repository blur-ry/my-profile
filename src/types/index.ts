export type Project = {
  id: string;
  title: string;
  description: string;
  link: string;
};

export type Skill = {
  id: string;
  name: string;
};

export type Education = {
  id: string;
  institution: string;
  degree: string;
  period: string;
  description: string;
};
